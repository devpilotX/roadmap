# Definition of Done

Mission: harden and complete The Roadmap Tracker, then deploy it live at
https://roadmap.devpilotx.com on the Oracle VPS 168.138.15.182, verified end to
end, and push the result to GitHub.

User constraint (2026-08-31 07:07): **do not run a local server.** Local
verification was therefore limited to typecheck, lint, build, the unit and
integration suites and static analysis. The HTTP surface suite was run against the
LIVE origin instead, which is stronger evidence than localhost.

| # | Criterion | Verification | Status |
|---|-----------|--------------|--------|
| 1 | Type check clean | `npm run typecheck` exit 0 | **PASS** |
| 2 | Lint clean, not on a deprecated runner | `npm run lint` → `eslint app components lib --max-warnings=0`, exit 0 | **PASS** |
| 3 | Production build succeeds | `npm run build` exit 0, locally and on the host | **PASS** |
| 4 | Unit + integration suite green | `npm test` → 519 tests, 459 pass, **0 fail** | **PASS** |
| 5 | Seed contract holds | `npm run verify` exit 0, 70 assertions, locally and on the host | **PASS** |
| 6 | No stale documentation | QA-REPORT, RUNBOOK, ADDITIONS, README rewritten; no reference to deleted files or removed dependencies survives unlabelled | **PASS** |
| 7 | Every API route reachable, every UI call resolves | static cross-audit: 0 missing routes; 6 orphan routes found and recorded in DEFERRED D-008 | **PASS (with D-008)** |
| 8 | Deployment artifacts complete | `deploy/` holds Caddy site, systemd unit, MySQL tuning, provision, release, verify-live, crontab — all committed and all executed | **PASS** |
| 9 | Live: TLS + health | Let's Encrypt certificate issued; `curl -sI https://roadmap.devpilotx.com/login` → HTTP/2 200 with HSTS `max-age=31536000; includeSubDomains` | **PASS** |
| 10 | Live: every page route | verify-live: 6 sampled pages 307 unauthenticated; smoke.mjs: **23 of 23 pages 200 with their own heading** signed in | **PASS** |
| 11 | Live: HTTP surface suite green | `PUBLIC_ORIGIN=https://roadmap.devpilotx.com` → **60 tests, 60 pass, 0 skipped** (previously all 60 skipped) | **PASS** |
| 12 | Live: database up and seeded | `/api/healthz` → `{"db":"up","env":"production","config_problems":0}`; `npm run verify` on the host exit 0 | **PASS** |
| 13 | Live: auth works end to end | probe account created over the API with CSRF, signed in, drove every page and endpoint, signed out with the session row deleted | **PASS** |
| 14 | Live: signup closes after first account | second signup → **403**; probe then deleted, 0 accounts remain so the owner's first signup creates theirs | **PASS** |
| 15 | Secrets: none in git, prod secrets generated on the host | secret scan clean across the branch; `SESSION_SECRET` and `TOKEN_ENC_KEY` generated by `openssl rand` on the VPS into `/etc/roadmap-tracker/roadmap.env` (0640 root:roadmap), never in the repo | **PASS** |
| 16 | Pushed to GitHub | `git push` succeeds, remote HEAD equals local HEAD | **PASS** |

## Evidence summary

```
LOCAL
  npm run typecheck   exit 0
  npm run lint        exit 0   (eslint CLI, not the deprecated `next lint`)
  npm run build       exit 0   ✓ Compiled successfully
  npm test            519 tests / 459 pass / 0 fail / 60 skipped (HTTP suite)
  npm run verify      SEED VERIFIED. 70 assertions passed, 0 failed.
  triggers.test.mjs   7 pass / 0 fail   (proves the CRITICAL trigger fix)

LIVE  https://roadmap.devpilotx.com
  http.test.mjs       60 tests / 60 pass / 0 skipped
  verify-live.sh      34 passed / 0 failed / 0 skipped
  smoke.mjs           23 of 23 pages, every read endpoint, both downloads, exit 0
  /api/healthz        {"ok":true,"data":{"db":"up","today":"2026-08-31",
                       "block":"LEARN","nextBlock":"BUILD","env":"production",
                       "config_problems":0}}
```

Deferred items are in `DEFERRED.md`, ten of them, each with an impact rating and a
next step. None of them is a broken feature; D-009 (no browser pass over the UI
changes) is the one worth acting on first.
