<%- include('../partials/head') %>

<div class="page-head">
  <h1 class="page-head__title">Everything A to Z</h1>
  <p class="page-head__lede">Every trackable item in the roadmap, in one list.</p>
</div>

<section class="stack" id="ev-summary" aria-label="The global number">
  <div class="card"><p class="muted">Loading the global number.</p></div>
</section>

<section class="stack-sm" id="ev-filters" aria-label="Filters and search">
  <div class="card"><p class="muted">Loading filters and search.</p></div>
</section>

<section class="stack" id="ev-groups" aria-label="Per group">
  <div class="card"><p class="muted">Loading per group.</p></div>
</section>

<section class="stack" id="ev-list" aria-label="Every item">
  <div class="card"><p class="muted">Loading every item.</p></div>
</section>

<%- include('../partials/foot') %>
