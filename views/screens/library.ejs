<%- include('../partials/head') %>

<div class="page-head">
  <h1 class="page-head__title">Resource library</h1>
  <p class="page-head__lede">Every link in Part 7, all twenty categories, each one tickable.</p>
</div>

<section class="stack" id="l-summary" aria-label="Library summary">
  <div class="card"><p class="muted">Loading library summary.</p></div>
</section>

<section class="stack-sm" id="l-filters" aria-label="Filters">
  <div class="card"><p class="muted">Loading filters.</p></div>
</section>

<section class="stack" id="l-list" aria-label="The links">
  <div class="card"><p class="muted">Loading the links.</p></div>
</section>

<%- include('../partials/foot') %>
